#!groovy
// Run docker build
properties([disableConcurrentBuilds()])
pipeline {
    agent { 
        label 'master'
        }
    
    
    stages {
        stage("Build dockerfile") {
            steps {
                echo "************ Start building image *****************"
                sh 'docker build .'
                }
            }
        }
        
    }
}